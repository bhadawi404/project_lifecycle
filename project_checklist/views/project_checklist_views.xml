<!-- project_checklists/views/project_checklist_views.xml -->
<odoo>
    <record id="action_project_checklist" model="ir.actions.act_window">
        <field name="name">Checklists</field>
        <field name="res_model">project.checklist</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="view_project_checklist_list" model="ir.ui.view">
        <field name="name">project.checklist.list</field>
        <field name="model">project.checklist</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="project_id"/>
                <field name="user_id" widget="many2one_avatar_user"/>
            </list>
        </field>
    </record>

    <record id="view_project_checklist_form" model="ir.ui.view">
        <field name="name">project.checklist.form</field>
        <field name="model">project.checklist</field>
        <field name="arch" type="xml">
            <form string="Checklist">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. Kickoff Meetiing"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="project_id"/>
                        </group>
                        <group>
                            <field name="user_id" widget="many2one_avatar_user"/>
                            <field name="note"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Checklist Lines">
                            <field name="line_ids" widget="one2many_list">
                                <list editable="top">
                                    <field name="sequence" widget="handle"/>
                                    <field name="code" />
                                    <field name="name"/>
                                    <field name="description" />
                                    <field name="note" />
                                    <field name="checklist_ids" widget="many2many_tags" readonly="1"/>
                                    <field name="assoc_employee_ids" widget="many2many_avatar_user" readonly="1"/>
                                    <field name="assoc_task_ids" widget="many2many_tags" readonly="1"/>
                                    <button string="Add line(s) to other checklist" name="add_line_to_checklist_wizard_action" type="object" class="oe_highlight"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
